const_guid! { CLSID: "ole";
	/// A COM class ID, from which the interfaces are created (`GUID`).
	=>
}

const_guid! { IID: "ole";
	/// A COM interface ID, which uniquely identifies the interface (`GUID`).
	=>
}
